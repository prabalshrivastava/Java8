package interview;

import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class BNY {

  public static void main(String[] args) {
    List<String> names = Arrays.asList("John", "John", "Tushar", "RAM", "Ram", "Sagar");
    Map<String,Integer> map = new HashMap<>();
//    names.stream().map(String::toLowerCase). do it in parallel streams
//    order payment seller notification are 4 microservices and notification can only be called once the order payment seller are returning a response(called in parallel and how to handle if any 1 microservice is failing
//    result should be :- {john = 2, tushar = 1, ram = 2, sagar = 1}]
//    how to do DB partitioning based on location
//    Indexing,views
//    template pattern.
  }
}
